# Intake handoff

Ready for the representative palace blockout. Strict quality validator passes and next.py reports `build-current-pass / blockout`, with 0 correction loops used.

The spec has 12 macro parts, 26 meso assemblies, 26 evidence-mapped local features, 11 procedural materials and five repetition systems. The first implementation must make the palace seven-spire hierarchy, deep arched entry, real gallery/stair depth and gold roof ribs legible inside its existing plot.

The spec is an adapter contract for the current runtime factory. Its macro and meso dimensions are independently normalized semantic envelopes. Generic factory generation alone is not a faithful implementation and is not a drop-in world replacement. Preserve canonical sockets, 36 tiles and all existing gameplay semantics.

The approved concept is a single generated architectural environment view. Suitability is conditional for the explicitly approved stylized redesign, not literal scene reconstruction. Hidden faces and material physics are authored inference. Projection and extracted PBR maps are explicitly inapplicable because all-angle lighting is required.

No V2 render, material pass, visual comparison, orbit check, action-ready check or Quality-Lord approval has been claimed. All downstream gates remain pending. `state.json` here is a durable intake snapshot; `.img2threejs/island-002-v2/state.json` continues as the working checklist.
