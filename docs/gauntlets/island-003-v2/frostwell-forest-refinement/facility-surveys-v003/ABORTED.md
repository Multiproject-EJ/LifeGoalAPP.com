# Aborted camera check

The reviewer found the first survey attempt V002 had captured the default overview because URL focus did not apply the special inspection preset. V003 used the same setup, so it was stopped before using any of its frames as facility evidence. V004 explicitly selects Frostwell and asserts the active preset.
