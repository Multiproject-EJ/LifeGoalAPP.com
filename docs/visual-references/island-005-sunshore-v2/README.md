# Island 005 — Sunshore Atoll V2 reference packet

The user approved the ImageGen concept and local upgrade on 2026-09-23. `005-source.png` is the artistic target, not implemented 3D or an exact gameplay blueprint. `manifest.v1.json` records the admitted source hash and linked evidence.

The fresh baseline is `../../gauntlets/island-005-v2/baseline-v002/capture.json`, captured from origin/main `6a07260f40dcfe9af082383e2c20368bab519325` before V2 geometry edits. Its overview, survey, two orbit and five landmark screenshots replace the archived proposal screenshot as the implementation comparison. This is desktop Chromium rendering a phone-sized canvas, not physical-device evidence. `baseline.v1.json` records the capture manifest hash and screenshot hashes.

Preserve the same Sunshore island, all five landmark positions, 36 canonical tiles, gameplay services and build progression. Improve limestone coves, planted shore, timber/thatch construction and readable interiors. Habit Lodge L1 remains a low deck/pergola, L2 gains upper veranda/roof, and L3 gains trim and planting within its footprint.

The approved concept's simple central arena differs from the latest runtime arena. The latest `createIsland5SunwheelArena` contract wins: the unfunded opening already has a presentation-complete arena while canonical Boss progress is Level 0; funded levels add ceremony around the open creature airspace. Preserve the creatureAirspace socket, open center, arena identity and no roof over Crown Drifter. Do not regress this runtime to the concept's simplified arena.

Single-view hidden rear and underside faces are inferred. The lit concept cannot support measured PBR or exact projected surface claims; procedural material response is an explicit stylized approximation. Nine exact grid crops support inspection and add no new view authority.

Intake and strict sculpt validation passed. Independent staged reviews have accepted macro blockout, supported structures, stylized form and material palette/readability. Surface, lighting, existing runtime interaction, production-route optimization and scoped assembly coverage are now accepted; physical-device testing remains unverified. Acceptance scope is recorded per pass; the approved concept is still not a claim of exact reconstructed pixels. See `.img2threejs/island-005-v2/state.json` at the repository root and the gauntlet's `part-ownership.json` for the planned 13-part hierarchy.
