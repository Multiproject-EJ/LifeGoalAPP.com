# Bloom card-pop intake

User requests a tiny rounded 4/10 figure, then same-pose image-to-3D reveal. Static botanical maquette, not a humanoid or skeletal rig. Design reference: concept-v2.png. Pose continuity uses a frozen render of the runtime model.

Observed: broad ellipsoid body, rounded rose plates, moss pads, short stalks and amber buds, oval mask with small slits. Pads overlap shell; stalk roots overlap crown; mask embeds into body. Rough dielectric clay. Back and underside inferred. No ears, muzzle or large eyes. Micro sculpt deliberately omitted.

Suitability conditional: single view supports approximate blockout; hidden anatomy and tender emotion uncertain. User explicitly permits 0.4 fidelity. Detailed PBR recovery/projection not applicable to flat-clay blockout. Later model passes remain open; UI tests do not approve artwork.

Interaction: family/form-specific study edition, identical model/camera/light for poster and first frame, lift from measured image rectangle, rotate after reveal, reset pose, reduced-motion instant reveal, WebGL failure retains image, close restores focus and disposes GPU. No save changes or generic model fallback.
