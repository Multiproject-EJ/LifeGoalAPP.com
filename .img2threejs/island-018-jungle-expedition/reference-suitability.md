# Island 018 Jungle Expedition - Reference Suitability

Verdict: conditional pass.

The supplied source is suitable for a stylized, source-faithful real-time
Three.js island environment because the macro silhouette, central temple,
palette, material families, waterfall systems, vegetation density, landmark
themes and camera mood are all visible. The technical probe passed at 853 x
1844 PNG and deterministic reference admission accepted the full source.

The reference is conditional rather than full-pass because it is a complete UI
screen, not a UI-free object turnaround. It includes baked text, counters,
buttons and fake tile art that must not be copied into runtime. It is also a
single front three-quarter view, so hidden sides and backs must be treated as
inference or generated secondary references.

Implementation target: preserve visible source identity in a UI-free runtime
world that fits the canonical Island Run `spark36_ring` board and phone view.
No gameplay, board topology, stop progression or HUD behavior should change to
match the concept image.
