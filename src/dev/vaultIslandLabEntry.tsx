import React from 'react';
import ReactDOM from 'react-dom/client';
import VaultIslandLab from './VaultIslandLab';

ReactDOM.createRoot(document.getElementById('root') as HTMLElement).render(
  <React.StrictMode>
    <VaultIslandLab />
  </React.StrictMode>,
);
